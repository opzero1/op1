{
  "name": "@opencode-harness/workspace-plugin",
  "version": "1.0.0",
  "description": "Project-scoped plan management for opencode-harness",
  "type": "module",
  "main": "workspace.js",
  "scripts": {
    "build": "bun build src/index.ts --outfile workspace.js --target bun --format esm",
    "typecheck": "tsc --noEmit",
    "dev": "bun run build --watch"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0"
  },
  "files": [
    "workspace.js"
  ]
}
