---
name: ui
description: 'Explore, build, and refine UI.'
---

This skill is managed remotely.

- If the direct `uidotsh_uidotsh_fetch` tool is available, use it to fetch `uidotsh://ui`.
- If ui.sh is routed through Warmplane/mcp0 instead, use `mcp0_*` discovery/call tools to invoke the `uidotsh.uidotsh_fetch` capability for `uidotsh://ui`.

Treat the fetched content as this skill's `SKILL.md` instructions and follow any additional `uidotsh://...` references it points to.
